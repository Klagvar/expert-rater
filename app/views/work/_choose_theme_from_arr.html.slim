= form_tag(display_theme_path, method: :post, remote: true, id: 'theme-select-form') do
  .form-group.mb-3
    = select_tag :theme,
            options_for_select(@themes, params[:theme]),
            class: 'form-control bg-dark text-light',
            prompt: t('work.index.select_theme_prompt'),
            onchange: "this.form.requestSubmit()"
